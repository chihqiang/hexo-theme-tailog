<footer class="bg-gradient-to-r from-purple-700 to-blue-600 text-white py-8 mt-auto">
  <div class="container mx-auto px-4">
    <div class="flex flex-col md:flex-row md:justify-between md:items-start space-y-6 md:space-y-0">
      <!-- 关于站点 -->
      <div class="md:w-1/4">
        <h3 class="text-xl font-semibold mb-3"><%= conf('title') %></h3>
        <p class="text-sm opacity-80">
          感谢您的访问！这里是 <%= conf('author') %> 专属博客，分享生活与技术。
        </p>
      </div>

      <!-- 快捷链接 -->
      <div class="md:w-1/4">
        <h3 class="text-xl font-semibold mb-3">快捷导航</h3>
        <ul class="space-y-2 text-sm opacity-90">
          <% let menu = conf('menu'); %>
          <% for (let name in menu) { if (menu.hasOwnProperty(name)) { %>
            <li>
              <a href="<%- url_for(menu[name]) %>" class="hover:text-yellow-300 transition">
                <%= name %>
              </a>
            </li>
          <% } } %>
        </ul>
      </div>

      <!-- 友情链接 -->
      <div class="md:w-1/4">
        <h3 class="text-xl font-semibold mb-3">友情链接</h3>
        <ul class="space-y-2 text-sm opacity-90">
          <% let friends = conf('friends') || []; %>
          <% friends.forEach(function(friend){ %>
            <li>
              <a href="<%- friend.url %>" target="_blank" rel="noopener" class="hover:text-yellow-300 transition">
                <%= friend.name %>
              </a>
            </li>
          <% }) %>
        </ul>
      </div>

      <!-- 联系方式 -->
      <div class="md:w-1/4">
        <h3 class="text-xl font-semibold mb-3">联系方式</h3>

        <p class="text-sm opacity-80 flex items-center space-x-2">
          <!-- 邮箱图标 -->
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-yellow-400 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8m-18 8h18a2 2 0 002-2V8a2 2 0 00-2-2H3a2 2 0 00-2 2v6a2 2 0 002 2z" />
          </svg>
          <a href="mailto:<%= conf('email') %>" class="hover:text-yellow-300"><%= conf('email') %></a>
        </p>

        <p class="text-sm opacity-80 flex items-center space-x-2 mt-2">
          <!-- 地址图标 -->
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-yellow-400 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 11c1.657 0 3-1.343 3-3S13.657 5 12 5 9 6.343 9 8s1.343 3 3 3z" />
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 21s8-4.5 8-10a8 8 0 10-16 0c0 5.5 8 10 8 10z" />
          </svg>
          <span><%= conf('address') %></span>
        </p>
      </div>


    </div>

    <hr class="my-6 border-purple-400 opacity-50" />

    <p class="text-center text-sm opacity-70 mb-2">
      © 2024 <%= conf('title') %>. 保留所有权利.
    </p>

    <!-- Hexo 和主题版权 -->
    <p class="text-center text-xs opacity-50">
      由 <a href="https://hexo.io/" target="_blank" rel="noopener" class="underline hover:text-yellow-300">Hexo</a> 驱动，主题由 
      <a href="https://github.com/chihqiang/hexo-theme-tailog" target="_blank" rel="noopener" class="underline hover:text-yellow-300">hexo-theme-tailog</a> 提供。
    </p>
  </div>
  <%- partial('analytics') %>
</footer>
